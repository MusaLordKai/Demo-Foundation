# Optional. By default the dev server proxies /api to http://localhost:4000
# (see vite.config.ts). Set VITE_API_URL to call an API on another origin
# directly (bypassing the proxy), e.g. VITE_API_URL="http://localhost:4000".
# VITE_API_URL=
# VITE_PROXY_TARGET="http://localhost:4000"
